// Hex values mirrored from tailwind.config.js.
// The <Divider> component needs real colour strings (they go into inline SVG
// fills), so every colour a band can be has an entry here.
export const palette = {
  mint: '#B8D6B2',
  haze: '#A7B0AA',
  pine: '#24463C',   // the footer seam only — no section uses it
}
