// Hex values mirrored from tailwind.config.js.
// <Divider> and <Logo> need real colour strings (they go into inline SVG
// fills), so every colour a band can be, and the bloom yellow, has an entry here.
export const palette = {
  mint: '#B8D6B2',
  haze: '#A7B0AA',
  pine: '#24463C',   // the footer seam only — no section uses it
  dandelion: '#EDD382', // the immortelle blooms in every seam
}
